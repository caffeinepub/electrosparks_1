{
  "frontend/src/App.tsx": "Main application entry point with TanStack Router setup; adds the new /dashboard route pointing to the Dashboard page component alongside all existing routes (home, technical, non-technical, register, payment, success, contact, admin).",
  "frontend/src/components/PhoenixIntro.tsx": "Image-based cinematic intro component that displays the VibECX-2K26 phoenix image full-screen with a fade-in + zoom animation (scale 1→1.05 over 7s), a pulsing orange radial glow behind the image, subtle upward-floating ember particles on a canvas overlay, and a smooth fade-out that calls onComplete after ~7 seconds.",
  "frontend/src/pages/Register.tsx": "Multi-field registration form that now calls the backend `submitRegistration` before navigating to the Payment page, showing a loading spinner during the async call and an inline error if it fails.",
  "frontend/src/pages/Payment.tsx": "Payment page that collects a payment screenshot upload and submits all registration data to the backend via `useSubmitRegistration`; fixes type errors by wrapping `year`, `numberOfMembers`, and `totalAmount` with `BigInt()` to match the hook's `bigint` parameter types.",
  "frontend/src/pages/Success.tsx": "Registration success confirmation page displaying participant name, event type, number of members, and total amount paid from context, with a \"Confirmation details will be sent via email\" message and a home navigation button.",
  "frontend/src/pages/Contact.tsx": "Fully redesigned premium glass UI Contact page with IIT-level glassmorphism aesthetic — features a glowing \"CONTACT US\" title with orange/red neon text-shadow, a wide college info glass card, separate staff and student coordinator glass cards in a responsive grid (each with circular icon, role badge, name, and phone), and a General Enquiries glass panel at the bottom; all text uses Times New Roman, with the student coordinator name updated to \"Ayyanar.G\".",
  "frontend/src/components/Footer.tsx": "Three-column footer with VibECX-2K26 branding on the left, Quick Links in the center, and a Contact column on the right showing Convenor, Faculty Co-ordinator, and Student Co-ordinators with phone numbers; the student coordinator name updated from \"Ayyanar\" to \"Ayyanar.G\", retaining dark background, neon orange/gold Times New Roman styling, and copyright line.",
  "frontend/src/pages/NonTechnicalEvents.tsx": "Non-Technical Events page displaying five event cards (AD-MAD, Jam, Dump Charades, Talent Sow, E-Sports) with the Dump Charades description updated to \"A classic game where one team member acts out a movie or technical term without speaking.\"",
  "frontend/src/components/CountdownTimer.tsx": "A self-contained live countdown timer component that counts down to March 10, 2026 at 23:59:59, updating every second via setInterval, displaying Days/Hours/Minutes/Seconds in large bold Times New Roman numerals with orange/red neon glow styling and a glassmorphism dark container.",
  "frontend/src/pages/Home.tsx": "Home page for VibECX-2K26 with hero section, stats, and events preview; the `.hero-title-section` wrapper now uses `transform: translateY(-40px)` (reduced from -90px, scale removed) on both desktop and mobile, the main \"VibECX-2K26\" title uses `font-size: clamp(58px, 8.5vw, 120px)` with `white-space: nowrap` on desktop, and a `@media (max-width: 768px)` override sets `clamp(38px, 7vw, 70px)` with `white-space: normal` for mobile wrapping. All colors, glow, font family, alignment, buttons, and background remain unchanged.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for backend operations — contains `useSubmitRegistration` mutation, `useGetAllRegistrations` query, `useGetStats` query, and `useDeleteRegistration` mutation that calls `actor.deleteRegistration(id)`, throws on `notFound`, and invalidates both `registrations` and `stats` query keys on success.",
  "frontend/src/utils/csvExport.ts": "Utility functions for exporting registration data to CSV — includes exportToCSV (triggers browser download), formatTimestamp (nanoseconds to readable date), and formatEventType (enum to display string).",
  "frontend/src/components/AdminDashboard.tsx": "Admin dashboard that first checks admin status via `useIsCallerAdmin`, shows a loading screen during actor/admin-check initialization, shows an \"Access Denied\" screen if the identity lacks admin privileges, and otherwise renders stat cards and a filterable/sortable registrations table with CSV export and detail preview modal.",
  "frontend/src/pages/Admin.tsx": "Three-step admin page at /admin — (1) frontend password gate, (2) Internet Identity connection step, and (3) a full dashboard that fetches all registrations via `getOpenRegistrations()` (corrected from the non-existent `getAllRegistrations`), displaying stats cards and a sortable table with all registration fields in the app's dark neon-cyan theme.",
  "frontend/src/pages/Dashboard.tsx": "Protected admin dashboard with sessionStorage-based password login; displays stats cards (Total Registrations, Total Members, Total Revenue) computed from live registration data, a full registrations table with an Actions column containing a red Delete button per row, a delete confirmation dialog with error handling, and an Export to Excel button above the table that downloads all current non-deleted registrations as an .xlsx file.",
  "frontend/src/utils/excelExport.ts": "Utility function `exportRegistrationsToExcel` that dynamically imports SheetJS from CDN, maps Registration objects to Excel-friendly rows with all required columns (Name, College, Dept, Year, Phone, Email, Event, Members, Amount, Date & Time), sets column widths, and triggers a browser download of `VibECX-2K26-Registrations.xlsx`.",
  "frontend/src/index.css": "Global stylesheet for VibECX-2K26 — sets the body background to royal beige (`oklch(0.83 0.04 65)`, ~#DCCBB5) as the visible page background with no dark overlay, keeps `#root` at `position:relative z-index:1`, and preserves all existing animations, neon utilities, font imports (Orbitron, Rajdhani, Share Tech Mono), design tokens, and input overrides unchanged."
}